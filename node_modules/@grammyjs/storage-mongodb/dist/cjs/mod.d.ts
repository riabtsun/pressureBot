import { StorageAdapter, Collection } from './deps.node.js';
export interface ISession {
    _id: {
        $oid: string;
    };
    key: string;
    value: unknown;
}
export declare class MongoDBAdapter<T> implements StorageAdapter<T> {
    private collection;
    constructor({ collection }: {
        collection: Collection<ISession>;
    });
    read(key: string): Promise<T>;
    write(key: string, data: T): Promise<void>;
    delete(key: string): Promise<void>;
}
